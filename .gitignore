# Local west workspace (probe builds only — release artifacts come from CI)
/.venv/
/.west/
/zephyr/
/zmk/
/modules/
/optional/
/zmk-pmw3610-driver/
/tools/
/bootloader/
/build/

# macOS metadata
.DS_Store

# Local build helpers
Makefile
scripts/build-local-artifacts.sh
local-build/
